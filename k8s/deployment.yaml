apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
  namespace: app-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
      annotations:
        rollout-trigger: "2026-02-10-1610"
    spec:
      containers:
      - name: my-app
        image: makbhavin23/my-app:f0cf763c63e9ca39d0b407c82c25ab4b0d3dfea8
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        env:
        - name: APP_ENV
          value: "production"
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 5
